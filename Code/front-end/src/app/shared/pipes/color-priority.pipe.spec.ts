import { ColorPriorityPipe } from './color-priority.pipe';

describe('ColorPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
