import { StarsPriorityPipe } from './stars-priority.pipe';

describe('StarsPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new StarsPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
